import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Map = props => {
    return (
        <View>
            <Text>MapScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default Map;
